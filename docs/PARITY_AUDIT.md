# ABIS Full-Parity Gap Report

> **⚠️ 2026-07-11: mostly closed since this was written. For the CURRENT open backlog (re-verified against
> today's code, with checkboxes to knock out one by one), see [`REMAINING_WORK.md`](REMAINING_WORK.md).**
> This file is retained as the historical full-detail audit.

> Generated by the multi-agent parity audit (workflow, 2026-07-07): 7 surfaces, 227 findings, 196 partial/missing gaps. Cross-checks the legacy ABIS PB app, DAS, handheld scanners, and cron/EDI against the greenfield rebuild. The "nothing missed" reference for the build phase. Live PL/SQL was re-exported the same day (docs/data-model/oracle_plsql_current.sql).

# ABIS / DAS / Scanner / Cron — Full-Parity Gap Report

Consolidated from 196 verified parity findings across seven surfaces. Every legacy path below is real and the greenfield state was grep-verified in the source findings. Ordered within each surface by severity, then **missing before partial**, then **core before nice-to-have**.

> **Build progress — 2026-07-07:** Root cause #1 (shape geometry) — **built end-to-end** at both levels. A uniform shape model + registry (`api/src/ABIS.Api/Data/ShapeGeometry.cs`) reads/writes all 10 per-shape dimension tables at the **order-item** level (`GET/PUT /api/orders/{o}/items/{i}/shape`, with dies) and the **part-master** level (`GET/PUT /api/parts/{id}/shape`, no dies), plus a `GET /api/lookups/shape-types` catalog and a dynamic **UI** (`/ui/shape-editor.html`, browser-verified). 176 tests green. Remaining for #1: wiring the captured geometry into the calculator/invoice/QC surfaces — done as each of those surfaces is built.
>
> **Customer master widened (`9f9ee99`):** `CustomerWrite` 5→36 fields incl. the 14 EDI/behavior control flags (`edi_req`, `create_861_at_receiving`, `plant_code`, …) — unblocks the 861-EDI gate.
>
> **Document/print layer started (`f3bd80a`, root cause #2):** dependency-free server-side HTML renderer (`Documents/HtmlDocuments`) with print CSS + inline Code 39 barcode, at `GET /api/documents/{sheet-skid,scrap-skid}/{id}` + `/ui/documents.html`. First documents = skid tags (browser/HTTP-verified). Reusable base for invoices/BOLs/certs.
>
> **Invoice built end-to-end (§1 rows 5–7):** the three commercial-invoice gaps are closed. **Save** — `INVOICE` model/repo/endpoints (`POST /api/accounting/invoices` + `GET` list/one, 201/400/404/409) over the composite `(ab_job_num, invoice_num)` key; the reserved-word `"TIMESTAMP"` column is quoted. **Document** — `HtmlDocuments.InvoiceDoc` + `GET /api/documents/invoice/{job}` renders the full billing document (customer/enduser/PO, shape+spec W×L via the shape-geometry primitive, alloy/temper/gauge, and net/unapplied/rejected/rebanded/processed/scrap/tare/offal wts & %). **Rejected-coil billed weight** — the naive browser sum is replaced by the exact legacy rule `MAX(shift-end-or-balance, prior-process-qty)` (`Data/InvoiceBilling` + `GET /api/accounting/invoices/{job}/computation`), sourced server-side; `/ui/accounting.html` now shows it. 195 tests green (incl. 7 pure-rule cases pinning every branch). **Live Oracle validation of the billing figures: see `docs/ORACLE_VALIDATION.md`.**

**Severity key**
- **C — Critical:** a core business/operational function that is absent or hollow; blocks a whole workflow or the ability to run the plant/business.
- **H — High:** core function only partially built (data model or read path exists, workflow/validation/write does not), or an important secondary function missing.
- **M — Medium:** operational depth, analytics, or a secondary workflow.
- **L — Low:** nice-to-have, UX polish, or already superseded by better platform behavior.

---

## Cross-cutting root causes (read this first)

Several "surfaces" fail for the **same** underlying missing primitive. Fix these once and dozens of individual gaps close:

1. **Per-shape geometry primitive (11 shape dimension tables).** Rectangle / circle / chevron / fender / parallelogram / trapezoid / L- & R-trapezoid / reinforcement / liftgate / x1shape. It threads through order entry, the part master, the piece-weight calculator, invoice spec strings, dimension-check QC tolerances, the DAS job sheet, and the stacker board. Greenfield stores only the shape *name* (`sheet_type`) and drops every dimension. This one gap appears independently in 5 surfaces.
2. **No document/print/output layer at all.** Invoices, quotes, packing lists, BOLs / combi-forms, skid & scrap tags, coil ZPL/CPCL labels, transfer certificates, and virtually every report have **no** greenfield producer. The modern stack can record data but cannot emit a single physical document.
3. **No EDI engine.** Generation (861/870/846/856/863), VAN transport (Inovis/GXS SFTP), inbound ingestion, and 997 reconciliation are all absent; `/edi` is a read-only ledger. This is the single largest functional block. **Constraint:** per the no-live-firing rule it must be single-owner — the legacy crontab on db01 stays the sole owner until a controlled cutover, so this is a re-home problem, not a green-field-and-run problem.
4. **No live shop-floor (DAS) capability.** The defining feature — real-time OPC DA/DCOM tag binding driving line state, counters, efficiency, and the operator workflow — is entirely absent; the modern "console" is a manual web form. Blocked architecturally by Classic OPC DA needing an edge/UA bridge.
5. **No handheld/RF scanner surface.** The Perl CGI coil-receiving station (scan → resolve/mint ABC → print label) has no greenfield home; all ~35 greenfield pages are desktop-oriented.
6. **Feature-gate enforcement wired to admin only.** The catalog and gate exist but are enforced on just 7 security-admin writes; every other domain endpoint is auth-only. This is the biggest security-parity hole.

---

## 1. Commercial (order entry, parts, sales/quoting, customers, accounting) — 37 gaps

| Sev | Feature | Status | Legacy | What greenfield needs |
|---|---|---|---|---|
| C | Per-item SHAPE geometry + 11 dimension tables | missing | `order_entry/w_order_entry.srw:868-1010`; `d_order_item_rectangle.srd` (+10) | Model & persist the 11 shape dimension tables on the order line; a `/shapes` write/read path. Every dimensioned blank is currently lost; also feeds invoice spec + part def. |
| C | Quote authoring — create a new quote | missing | `sales/w_new_quote.srw`, `w_new_quote_selection.srw` | `POST /sales/quotes`. Today you can only view quotes and log CRM activity — you cannot originate one. |
| C | Quote editor — spec/pricing/inventory/shipment tabs | missing | `sales/w_edit_quote.srw` | `PUT /sales/quotes` plus the full tabbed body incl. coil/skid/scrap inventory and shipment linkage. |
| C | Quote pricing / cost model ($/lb, scrap-handling, job cost, ROS) | missing | `quotation/w_circlepro.srw:1480-1528, 1745-1784, 2475-2514` | Actual costing math (men×hrs + DEI + interleaf, scrap-handling charge, price/lb). Greenfield outputs yield% only — no dollars, so it is not a quote. |
| C | CirclePro coil-processing yield economics | missing | `quotation/w_circlepro.srw:1785-2451` | Avg/max coil weight, feed length, coil-end loss, annealing — the bulk of the 156KB window. Greenfield does plate-blank geometry only. |
| C | Invoice document generation (weight rollups + spec block) | **built** | `accounting/w_invoice.srw:109-360`; `d_report_invoice_data.srd` | `HtmlDocuments.InvoiceDoc` + `GET /api/documents/invoice/{job}`: customer/enduser/PO, shape+spec WxL, alloy/temper/gauge, and net/unapplied/rejected/rebanded/processed/scrap/tare/offal wts & %. |
| C | Invoice save (number + date) and print | **built** | `accounting/w_invoice.srw:14-56` | `POST/GET /api/accounting/invoices` persists the `INVOICE` row (number/date/notes, composite key); the printed invoice is the document above. |
| C | Piece-weight calculator (metal_density × shape geometry) | missing | `order_entry/w_order_entry.srw:691-829` | Compute suggested theoretical_unit_wt from density × gauge × per-shape area. Greenfield only stores a hand-typed number. |
| H | Edge-trim tolerance validation + override + audit | partial | `order_entry/w_order_entry.srw:496-641` | Enforce 1.5"–12" trimmer tolerance, override confirmation, and `f_add_system_log_tran` audit on save. Fields are carried but never gated — out-of-tolerance saves silently. |
| H | Sector consistency validation | missing | `order_entry/w_order_entry.srw:470-581` | Block save when a line lacks a sector; warn on mixed sectors. Sector is currently just an optional int. |
| H | Edit existing order + lines in the UI | partial | `order_entry/w_order_entry.srw:2396, 3952-3967` | Wire order-entry.ts to the existing PUT endpoints for in-UI add/edit/delete of lines on an existing order (currently create+view only). |
| H | Part revisions (version + re-point open items) | missing | `part_num/w_part_num_management.srw:529-679, 852-892` | Revision concept (`part_num_id_old→new`), carry routing forward, re-point open order items. Saves overwrite in place today. |
| H | Work-center / routing sequences per part | missing | `part_num/w_part_num_management.srw:817-892` | Ordered routing-step list per part (2024 feature). No endpoint/field exists. |
| H | Part-master shape geometry | missing | `part_num/d_pn_rectangle_new.srd` (+10); `w_part_num_management.srw:1468-1560` | Same 11-shape dimension capture on the part master; `PartWrite` drops it. |
| H | Customer master full field coverage + delete | partial | `oracle_ddl.sql:1915-1972` | Write the ~35 missing columns (street/country/phone/fax/notes, type, parent hierarchy, tax/exemption/DUNS, bill-to). `CustomerWrite` covers 5 of ~40. |
| H | Customer EDI / behavior control flags | missing | `oracle_ddl.sql:1941-1954` | Make `edi_req/desadv_req/qr_code_req/validate_material/coil_cert_label_req/create_861_at_receiving/…/plant_code` writable — they drive all downstream EDI/receiving/label/ship behavior. |
| H | Quote internal review / approval workflow | partial | `d_sales_quote_internal_review.srd` | Write path to record sales/VP/CEO sign-off. Approvals are display-only today, so the workflow is not executable. |
| H | Rejected-coil billed-weight derivation | **built** | `accounting/w_invoice.srw:509-536` | `Data/InvoiceBilling.RejectedCoilBilledWeight` reproduces `MAX(shift-end-or-balance, prior-process-qty)` exactly; sourced server-side (`GetInvoiceCoilsAsync` now carries the prior-process term + `BilledWeight`). Browser no longer sums `process_end_wt`. |
| H | Assign customer coils to an order | missing | `order_entry/w_cust_coil_list.srw`; `w_order_entry.srw:3043-3120, 942-968` | `/orders/{id}/coils` with duplicate `coil_org_num` warning. Greenfield links coils to jobs only, not to orders at entry. |
| M | Part obsolete workflow with in-use guard | partial | `part_num/w_part_num_management.srw:472-528, 893-1010` | Server-side block on obsoleting a part still referenced by an open order item. Flagging exists; guard does not. |
| M | Part delete | missing | `part_num/w_part_num_management.srw:245-413` | `DELETE /parts`. |
| M | Copy an existing part into a new part | missing | `part_num/d_new_part_copied.srd` | Clone-part (incl. shape) starting point. |
| M | Copy / duplicate an existing order | missing | `order_entry/w_order_entry.srw:667-689, 2539-2547` | Clone header+items+coils into a new order. |
| M | End-user change cascade on an existing order | partial | `order_entry/w_end_user_change.srw` | Dedicated re-point flow cascading enduser_part_num/labeling. Set-on-create exists; cascade does not. |
| M | Part-number picker launched from order entry | partial | `order_entry/w_order_entry_part_filter.srw`, `w_order_entry_pn_list.srw` | In-line part picker that pulls a defined part's spec. Users free-type spec per line today. |
| M | SheetPro rectangular-sheet quotation | missing | `quotation/w_quotation_new.srw`; `d_report_sheetpro.srd` | Second calculator for rectangular sheets (calc/print/save/open, per-piece price). |
| M | Quote-calculator persistence + print | missing | `quotation/w_circlepro.srw:622-684, 1529-1723` | Save/reload named quotes and print circpro2/sheetpro reports. Greenfield quotation is stateless client-side math. |
| M | Print quote / sales reports | missing | `sales/w_sales_printout.srw`; `d_sales_quote_report.srd` | Quote print/report output. |
| M | Email quote to customer | missing | `sales/w_sales_quote_to_cust_email.srw` | Email the quote to the customer contact. |
| M | Edge-trim tolerance maintenance screen | partial | `part_num/w_edge_trim_tolearance.srw` | Surface the dedicated trimmer-tolerance table/maintenance (part carries +/- fields only). |
| M | Part-number reports (per-shape spec sheets) | missing | `part_num/w_report_part_num.srw`; `d_report_pn_item.srd` | Printable part spec-sheet report. |
| M | Accounting scrap-type summary | missing | `accounting/d_acct_scrap_type_list.srd`, `_summary.srd` | Accounting-side scrap-type breakdown (distinct from the reporting scrap summary). |
| M | Print coil-cert label on order close | missing | `order_entry/w_order_entry.srw:2547-2566` | Cert-label print at close. |
| L | Sales calendar / scheduled reminders view | partial | `sales/w_sales_calendar.srw`; `d_sales_scheduled_event_list.srd` | Calendar view + due-reminder surfacing. Events are stored but not surfaced. |
| L | Sales catalog (catalog items for quoting) | missing | `sales/w_sales_catalog.srw` | Catalog module + quote-from-catalog selection. |
| L | Sales preferences | missing | `sales/w_sales_preferences.srw` | Per-user sales screen prefs. |
| L | Customer delete | missing | `oracle_ddl.sql:1915` | `DELETE /customers` (consistent no-delete pattern). |

---

## 2. Coils & Receiving (BOL receiving, coil inventory, skids, warehouse, ownership transfer, coil-eval) — 35 gaps

| Sev | Feature | Status | Legacy | What greenfield needs |
|---|---|---|---|---|
| C | Inbound EDI 856/ASN → BOL coil-line import | missing | `coil_receiving/w_coil_receiving.srw` (dw_incomingedi, `of_incomingedi:1350`) | Ingest `inbound_coil`/`inbound_shipment_status` EDI rows into the BOL. Greenfield mint works off manually-entered lines only. |
| C | Update inbound_shipment/coil status on receipt | missing | `coil_receiving/w_coil_receiving.srw:614-640` | Mark shipment received + each inbound coil consumed with damage codes (STATUS 3/1, received_time). |
| C | 861 Receiving Advice generation + resend/archive | partial | `coil_receiving/w_coil_receiving.srw` (`wf_create_861_4customer`, `ue_send861:1026`) | Real 861 build/resend/send-and-archive/delete-EDI. `generate-861` is a documented stub returning "deferred". (Keep single-owner per no-live-firing.) |
| C | Receiving-dock scanner station | missing | `receiving/w_receiving_dock.srw` (`ue_entercoil:341`, `wf_edi:595`) | Barcode-driven dock receiving: scan MID/org id, EDI cross-check, dup-id disambiguation, per-line customer, enter-existing-coil, save+label. Entirely unbuilt. |
| C | Warehouse skid CRUD + status-20 warehouse-coil mint | missing | `warehouse/w_wh_business.srw` (`pfc_save:764`, `wf_get_coil_info:532`) | Create/modify/delete warehouse skid + items, mint warehouse coil (status 20) + process_coil/sheet_skid/production_sheet_item/detail, weight recon, Arconic package-num. |
| H | Mint carries full coil attributes to COIL | partial | `coil_receiving/w_coil_receiving.srw` (`ue_savebol:378-530`); `AbisRepository.cs:2191` | Carry cash_date, part_num, supplier_sales_num, PO, consumed_coil_num, material_num, coil_location, mill-source, vo, cntry_of_cast, lfeed, mid_num, damaged_code. Mint drops all of these today. |
| H | QR / coil-quality barcode parsing (+flaw mapping) | missing | `coil_receiving/w_coil_receiving.srw` (`wf_coil_quality:1666`); `inv_coil/w_qr_receive.srw`, `w_qr_manual.srw` | `coil_quality`/`coil_quality_flaw_mapping` tables + QR Receive/Manual/Data screens. `customer.qr_code_req` path fully unimplemented. |
| H | Coil-ownership transfer — correct mint semantics | partial | `coil_ownership/w_coil_ownership_transfer.srw` (`wf_coil_ownership_transfer:201`) | MINT a NEW `coil_abc_num` for the new owner (status 2) and set original to status 13 (transferred). Greenfield just UPDATEs `customer_id` in place — wrong ownership audit trail. |
| H | Bulk "Change Status → Ready for Transfer" (status 12) | missing | `inv_coil/w_inv_coil.srw` (`cb_change_status:1664`) | Endpoint to set `coil_status=12` (multi-select). It is the precondition the transfer picker filters on, so the transfer flow is un-drivable end-to-end today. |
| H | Job recap / yield computation | missing | `coil_eval/w_recap.srw`; `d_coil_evaluation_coil_yield.srd` | Yield %, reband/reject/scrap/skid weight reconciliation that closes out a job's coils. No endpoint. |
| H | Coil-eval real-time QC station (OPC feed) | partial | `coil_eval/w_qc_sheet.srw` (OPC ondatachanged `:426`) | Real-time OPC-driven QC sheet, shift/job treeview, coil-queue selection. Greenfield returns coils-for-a-job only. (OPC = Classic DA/DCOM.) |
| H | Modify / delete sheet skid + items | missing | `skid_entry/w_office_skid_entry.srw` (`wf_check_skid_wt:788`) | PATCH/DELETE sheet skids with weight & piece reconciliation. |
| H | Scrap skid lifecycle (delete, return-scrap, credit) | missing | `skid_entry/w_office_skid_entry.srw` (`ue_new_scrap_skid:456`, `wf_scrap_credit:405`) | Delete scrap skid/item, return-scrap item, scrap credit to customer order. Only create exists. |
| H | Change coil customer on a received BOL (cascade) | missing | `coil_receiving/w_change_cust.srw` | Cascade the BOL header customer change to re-point minted coils' `customer_id`. PUT edits header only (Feature 1108). |
| H | Delete coil from inventory (guarded) | missing | `inv_coil/w_inv_coil.srw` (`ue_delete:420`, `wf_coil_used:998`) | DELETE with intransit-status-5-only + coil-used guard. No delete endpoint at all. |
| M | Force/validate cash date per alloy | missing | `coil_receiving/w_coil_receiving.srw` (`wf_validate_cash_date:2468`) | `customer.cash_date_required` enforcement + per-alloy heat-treatment cash-date save. |
| M | Duplicate ABC coil-number validation | missing | `coil_receiving/w_coil_receiving.srw` (`wf_coil_abc_num_duplicate:1598`) | Collision guard if a caller supplies an existing id (server assigns from sequence, but no guard). |
| M | Print coil barcode labels (Zebra preview) | missing | `coil_receiving/w_coil_receiving_barcode_printing.srw` | Label-printing endpoint/artifact. None anywhere. |
| M | Print coil label to handheld via CGI | missing | `coil_receiving/w_coil_receiving.srw` (`ue_print_label_to_handheld:1002`) | Scanner-print path (POST to coil_receiving.pl). See §6. |
| M | Manual new-coil entry + live scale weigh-in | partial | `inv_coil/w_coil_detail_new.srw`; `w_scale_coil.srw` | Full detail-entry form + physical scale integration (gross/tare off barcode scale). POST /coils exists but bare. |
| M | Coil-eval report + scrap-type pie charts | missing | `coil_eval/w_report_eval.srw`; `d_scrap_type_pie_chart_*.srd` | Eval reporting/visualization (distinct from job-level scrap summary). |
| M | Warehouse skid browser + auto-fill + detail entry | missing | `warehouse/w_wh_business.srw` (`open:725`, `wf_load_original_data:577`); `w_wh_detail.srw` | The whole warehouse-business browser (skids blanked by other tollers), Load-Orig-Data (Feature 1715), detail window. Greenfield is a single location/ticket/status PATCH. |
| M | Arconic sheet-skid package-number assignment | missing | `skid_entry/w_office_skid_entry.srw` (`wf_assign_package_num_2sheet_skid:402`) | `f_insert_sheet_skid_package` numbering (also from warehouse). |
| M | Skid on-hold reason + group status edit | missing | `skid_entry/w_office_skid_entry.srw` (`wf_check_onhold_reason:428`); `w_skid_group_edit.srw` | On-hold reason capture/track + bulk skid status/group edit. |
| M | QA customer-quality skids + email/export subsystem | missing | `skid_entry/w_office_skid_entry.srw` (`wf_save_qa_customer_quality_many_skids:422`); `w_qa_email_group.srw` | Defect disposition (ALBL/customer), QA email groups, SMTP send, Excel/report export. (Overlaps §4 quality.) |
| M | Barcode-scan ownership-transfer station | missing | `coil_ownership/w_coil_barcode_ownership_transfer.srw` | Scanner-driven transfer variant. |
| M | Import-from-BOL + Show-Archived-BOL browsers | missing | `inv_coil/w_inv_coil.srw` (`ue_import:492`, `ue_show_archived:528`) | Browse `inbound_shipment_customer` BOLs to import coils; view archived BOLs. |
| L | Reprint sheet/scrap skid tags | missing | `skid_entry/w_reprint_skid_tag2_oe.srw`; `w_sheet_skid_print_preview_oe.srw` | Skid-tag reprint/label output. |
| L | Search BOL by number / received-date range | partial | `coil_receiving/w_coil_receiving.srw` (`ue_searchbol:828`) | Free-text BOL lookup + date-range search across saved BOLs and incoming EDI. Filters by customerId+status only. |
| L | Coil inventory: prior-years + all-customer modes | partial | `inv_coil/w_inv_coil.srw` (`ue_search_retrieve:1754`) | First-class "prior years" archival toggle + all-customer aggregate mode. |
| L | Multi-condition coil search builder | partial | `inv_coil/w_inv_coil.srw` (`ue_set_search:275`, `cb_search:2549`) | Arbitrary column/operator/value filter chains. Only fixed query params today. |
| L | Skid inventory browser (history, prior-years, pre-recap) | partial | `inv_skid/w_inv_skid.srw` | Customer-scoped skid inventory with per-skid history, prior-years, pre-recap. |
| L | Transfer certificate reprint picker + ledger export | partial | `coil_ownership/w_reprint_coil_transfer_certificate.srw` | Dedicated reprint picker + ownership-transfer report/ledger. Cert fetch itself works. |
| L | Received-date sanity warning (future / >7 days) | missing | `coil_receiving/w_coil_receiving.srw:357-363` | Minor UX validation. |

---

## 3. Shop-floor DAS (live data acquisition, line control, downtime, stacker, shift lifecycle) — 31 gaps

This surface is the least covered. The greenfield "das-console" is a manual web form; the defining real-time capability does not exist. Note the architectural constraint: plant OPC is Classic OPC DA/DCOM and needs an edge/UA bridge before any of the "live" items are buildable.

| Sev | Feature | Status | Legacy | What greenfield needs |
|---|---|---|---|---|
| C | Live OPC/PLC data-acquisition binding | missing | `da/w_da_sheet.srw` (ole_1 ondatachanged) | Real-time bind to ~20 PLC tags (abcoil/abjob/abshift/automode/autorunning/goodpartcnt/rejectpartcnt/strokecnt/feedlength/activefault…) mapped from downtime.ini. This is the core of DAS and is wholly absent. |
| C | LINE_CURRENT_STATUS live line board | missing | `da/w_da_sheet.srw` (`wf_set_opc_abjob/abcoil/abskid`) | Read/write the live line-state record (job/coil/shift, LINE_STATUS, 19 skid locations, 2 stacker skids). Greenfield never touches `LINE_CURRENT_STATUS`. |
| C | Current-coil binding to job+shift (cross-shift carry) | missing | `da/uo_shift.sru` (`of_set_coil_id`) | `u_coil.init(coil,job,shift)`, apply feed/reject lengths, resume a mid-run coil at shift init. No concept of a live current coil. |
| C | Operation Panel workflow (New/End coil, End shift, Change job) | missing | `da/w_da_sheet.srw` (`w_operation_panel`) | The operator's primary guided action surface, auto-opened when a coil enters the feeder. None of these actions exist. |
| C | Real-time production counters from PLC | missing | `da/w_da_sheet.srw` (st_goodpartcnt/st_scrapcnt/st_stroke…) | Live good/reject/stroke/part-no/feed-length reads; post count deltas to the current coil. Greenfield captures none. |
| C | Coil-done persistence to SHIFT_COIL / SHIFT_PROCESS_STATUS | missing | `da/uo_shift.sru` (`wf_done_coil`) | Write the per-shift processed-coil ledger. Repo never references SHIFT_COIL or SHIFT_PROCESS_STATUS. |
| C | Coil barcode scan-to-load + actual-weight update | missing | `da/w_scan_coil_id.srw` | Scan a coil ("2S" prefix stripped), match against the job's queued coils, update `ABCO_COIL_NET_WT` on confirm. Greenfield ScanLog is a generic append-only note log — it resolves nothing. |
| C | Skid/scrap/sample ticket + Zebra label printing | missing | `da/u_da_skid_tabpg.sru` (`of_print_skid_ticket`) | Physical label print at weigh time (`d_report_coil_barcode_zebra*`, sample sticker) + reprint. Greenfield saves skids but prints nothing. |
| H | Line auto-status controls + live state | missing | `da/w_da_sheet.srw` (`u_line_status_button`) | Reflect/toggle Master Auto / Auto-running / Enable-Disable / Coil-on-table / Coil-in-feeder; noauto write disables the press. |
| H | Fault/health status indicators (DB/OPC/PLC lamps) | missing | `da/w_da_sheet.srw` (`u_fault_status_button`) | DB heartbeat (SELECT 1 FROM DUAL), OPC _ErrorCode, PLC activefault lamps; onerror halt-and-save. No health lamps today. |
| H | Live shift efficiency % (5s cadence) | missing | `da/w_da_sheet.srw` (`ue_refresh_display`) | Continuous (runtime − downtime)/runtime × 100 from shift start + DT_INSTANCE sums. Greenfield has an offline report only. |
| H | Live coil yield % + finish-% progress bar | missing | `da/w_da_sheet.srw` (`of_get_yield`, `of_get_finish_percent`) | Running-coil yield (red <95%) and finish progress; auto-enable end-coil past 90%. Greenfield shows static rows only. |
| H | End-coil recap (status + closing weight) | missing | `da/w_da_sheet.srw` (`ue_coil_offline`) | Capture ending status & net weight (w_recap/w_done_coil), save coil, clear line. No coil-close step. |
| H | Drop/reverse a wrongly-loaded coil (+ERROR_EVT) | missing | `da/uo_shift.sru` (`of_drop_current_coil`) | Null SHIFT_PROCESS_STATUS, delete SHIFT_COIL row, clear line/coil status, insert ERROR_EVT. No reversal path. |
| H | Change job on the same coil (split & save remaining wt) | missing | `da/uo_shift.sru` (`wf_change_job_same_coil`) | `u_coil.split_and_save(1,nw,job)` + stamp AB_JOB start. No split/change-job-mid-coil. |
| H | Per-line job queue / LINE_PRIORITY sequencing | missing | `da/w_da_sheet.srw` (`wf_set_opc_abjob`) | Rotate LINE_PRIORITY.STATUS + coil-queue tab per line. Greenfield lists ab_job by status but never reads/writes LINE_PRIORITY. |
| H | Automatic downtime capture when the line stops | missing | `da/uo_shift.sru` (`ue_line_idle`) | Auto-create a downtime instance the moment autorunning drops; end on resume. Greenfield downtime is 100% manual. |
| H | Downtime cause taxonomy + detail (DT_INSTANCE_DETAIL) | partial | `da/u_causes.sru` | Write DT_INSTANCE_DETAIL (cause_id from ~24 buttons + per-cause duration, concurrent causes). `DowntimeInstanceWrite` writes DT_INSTANCE + one free-text note; the DT_CAUSE lookup is unused on write, so cause-coded analytics are lost. |
| H | Stacker dual-station automation | missing | `da/w_da_sheet.srw` (stacker110.station1/2 tags) | OPC-driven stack-complete/send-blank/slave/leaving into SHEET_SKID_STACKER_1/2. |
| H | Shift lifecycle automation (auto new/end, DT_TOTAL) | partial | `da/w_da_sheet.srw` (`wf_new_shift/wf_end_shift`) | Schedule-driven auto start/end (+grace), START/END_TIME stamps, DT_TOTAL rollup, LINE_CURRENT_STATUS reset. Greenfield Shifts is hand-filled CRUD. |
| M | Stacker physical board (shape displays + conveyor cells) | missing | `stacker_110/w_110_stacker_read_only.srw` | 11 part-shape displays + ~16 conveyor-location cells + live stack tracking. Greenfield "board" is a list of ab_job rows. |
| M | Downtime operator prompts (other-cause, 1-min nag, resume, change-job) | missing | `da/u_dt_cause_tabpg.sru` | The disciplined downtime-coding prompts. None exist. |
| M | Shift override (extend end time) + supervisor validation | missing | `da/w_da_sheet.srw` (`wf_shift_override`) | Push scheduled end via supervisor gate; write abshiftoverride. |
| M | Supervisor / role validation + shift PIN codes | missing | `da/w_da_sheet.srw` (is_*_password) | On-console operator role codes/PINs gating exit/override/drop-coil/maintenance. Greenfield security is per-API only. |
| M | Maintenance tab: enable/disable line auto (lockout) | missing | `da/u_da_maintenance_tabpg.sru` | Write noauto to lock line out. Greenfield "Maintenance" is unrelated maint_log CRUD. |
| M | Partial-skid workflow (save partial/single, theo-wt at coil end, non-conforming) | partial | `da/u_da_skid_tabpg.sru` | Partial-save/carry-forward, theoretical wt at coil end, non-conforming capture. Greenfield lists partial skids read-only. |
| M | Scrap workflow depth (type picker, per-item, scale, return) | partial | `da/u_da_scrap_tabpg.sru` | Scrap-type picker + per-item detail + scale weight + return-scrap. Greenfield saves a flat ScrapSkidWrite (raw type number). |
| M | Serial scale/gauge acquisition (WSC32) | partial | `da/w_da_sheet.srw` (`wf_read_scale/wf_zero_scale`) | RS-232 zero/read commands, scrap-scale/gauge separation, device parsing. Greenfield GETs a single value from an optional edge URL. |
| M | Skid dimensional check wired into the weigh path | partial | `da/w_dimensional_check.srw` | Wire the per-skid dimensional check into the DAS skid-weigh flow. Endpoints exist but live in the QC-sheet surface, not DAS. |
| M | Offline manual-entry console (da_offline) | partial | `da_offline/w_da_offline_sheet.srw` | Add shift+job+coil selection and end-coil/end-skid guided steps. Greenfield console is closest to this but covers only weigh-skid/scrap/downtime-note. |
| M | Production e-folder / live job sheet | **BUILT** | `coil_eval/u_tabpg_job_sheet.sru` + `downtime2/d_report_prod_order.srd` | `GET /prod-folder/jobs/{job}/job-sheet`; rendered on the folder and the DAS console, re-read on job change; printable. **The legacy pointer in this row was wrong:** `da/w_da_sheet.srw`'s `wf_show_job_sheet` is an abandoned copy with every display call commented out. The live one is in coil_eval. |

---

## 4. Quality (863/mechanical certs, recovery, dimension QC, QA hold) — 25 gaps

| Sev | Feature | Status | Legacy | What greenfield needs |
|---|---|---|---|---|
| C | 863 / mechanical test-result WRITE (PST_TEST_RESULT) | missing | `edi/w_edi_863.srw:863` | A write path to post mechanical results. This is the *only* producer in legacy; greenfield's `/test-results` list can never be populated without it. Core hole. |
| C | Recovery scrap-worksheet data entry (RECOVERY_SCRAP_WORKSHEET) | missing | `quality/w_recovery.srw:746, 824` | Per coil/job, enter defect wt+pieces against the customer's tracked defect list and upsert the worksheet. Greenfield Quality tag is 4 read-only lookups. |
| C | Dimension-check tolerance validation vs part-shape spec | missing | `coil_eval/u_tabpg_skid_dim_check.sru:161, 375, 1124` | Enforce per-shape engineering tolerances (nominal ± plus/minus, 15% sanity band). Greenfield stores `in_spec` = whatever the client sends — no QC gate at all. |
| C | Coil QA hold workflow (status 11) + COIL_TRACK_QA | missing | `qa/w_new_qa_coil.srw:106`; `qa/w_qa_coil.srw` | Put a coil on QA hold and record the transition in COIL_TRACK_QA (from→to, by, note, date). PATCH can set a status code but there is no QA-hold workflow or audit trail. |
| H | Instron test-file (.ASC) import & parse | missing | `edi/w_edi_863.srw:992` | Fixed-field parse of coil/pos/YTS/UTS/elong/N/R/width/thickness for up to 9 samples. Entire ingestion absent. |
| H | EDI 863 cert-file generation (5 test types, N/R flags) | missing | `edi/w_edi_863.srw:833` | Generate outbound 863 per coil (EDI_FILE_863). Greenfield EDI has ledger only. |
| H | 863 coil/position validation against COIL | missing | `edi/w_edi_863.srw:453` | Resolve `coil_org_num→coil_abc_num` (cust 1153/1459), validate positions 11-15/21-25, flag errors. |
| H | Recovery reports suite (7 types, per-customer templates) | missing | `quality/w_recovery.srw:1210`; `d_report_recovery_*.srd` | product_type_summary, scrapdefectdetails, rej_coil_yearly, historical_data, yearly_summary + customer-specific dailies. None exist. |
| H | Recovery customer report SETUP | missing | `quality/w_customer_recovery_report_setup.srw` | Create/edit `recovery_report_customer` / `cust_scrap_type_needed`. Greenfield reads them but offers no write. |
| H | Coil QA hold search/browse console | missing | `qa/w_qa_coil.srw:188` | Filter QA-hold coils by all/selected customer, current vs historic, 30/60/180/all days, ABC/orig #. No coil_track_qa surface at all. |
| H | Job-level dimension QC graphic board (green/red) | missing | `coil_eval/u_tabpg_job_dim_check_gr.sru` | Per-job visual pass/fail across all skid dimension checks (gauge/width/len-op/len-dr). |
| M | 863 manual correction of coil#/position + re-check | missing | `edi/w_edi_863.srw:1071` | Operator override of parsed coil/position for failed rows + re-validate. |
| M | 863 resend | missing | `edi/w_edi_863.srw:1182` | Re-transmit a previously sent 863 batch. |
| M | Best-Before-Day calc + low-BBD email (6111 T4P) | missing | `edi/w_edi_863.srw:893` | Aging/BBD formula for age-hardening tempers + email QA/metallurgy when retest needed. |
| M | 863 test-file archival (.ASC → .read) | missing | `edi/w_edi_863.srw:960` | Rename processed instron files so they aren't re-imported. |
| M | Recovery: add / remove coil-job to a report | missing | `quality/w_recovery.srw:685, 656` | Select/deselect coils-jobs in a date range for a customer's recovery report. |
| M | Recovery autoparts vs non-autoparts filtering | missing | `quality/w_recovery.srw:854` | Filter the defect input list by the job part's autoparts flag. |
| M | Recovery: pull existing defect from DAS vs office | missing | `quality/w_recovery.srw:900` | Pre-populate the worksheet from prior office entry or DAS-recorded scrap. |
| M | Recovery report email / print / export / file tab | missing | `quality/w_recovery.srw:1273-1296` | Email/print/export recovery reports + per-customer attachment browser. |
| M | QA coil photos (add/delete/view JPG per coil) | missing | `qa/w_qa_coil.srw:250` | Per-coil QA photo gallery on the fileshare. |
| M | QA email notification + "make scrap" action | missing | `qa/w_qa_coil.srw:1163, 502` | Email a QA-hold coil's details; convert held material to scrap from the console. |
| M | Coil QC dimension check — edit & delete | partial | `coil_eval/u_tabpg_skid_dim_check.sru:884, 827` | Add update + delete endpoints (with pending-change guards). List/create exist only. |
| M | Good-material checks (in-spec rollup per coil) | missing | `coil_eval/d_good_material_checks.srd` | Roll dimension-check in_spec results up to good pieces per coil. |
| L | Dimension-check PC-number auto-increment per skid | missing | `coil_eval/u_tabpg_skid_dim_check.sru:971` | Auto-assign next pc_number per skid; today relies on client-supplied value against a unique constraint. |

---

## 5. Logistics & EDI (transport, transaction sets, shipping/packing, dies, sketches) — 23 gaps

Constraint reminder: EDI generation/transport must remain **single-owner** (legacy crontab on db01) until a controlled cutover — the gaps below are real, but the resolution is re-homing under one owner, not standing up a parallel firing path.

| Sev | Feature | Status | Legacy | What greenfield needs |
|---|---|---|---|---|
| C | EDI transport / VAN file transfer (GXS SFTP) | missing | `cron/db01-prod/scripts/GXS.ksh` | The whole inbound/outbound transport to Inovis/GXS (partner 412992496), postpro, VAN purge. Greenfield has ZERO transport. Single largest EDI gap. |
| C | Packing-list line items (what's on the shipment) | missing | `oracle_ddl.sql:8736` | SHEET/SCRAP/REJECT_COIL/WH_PACKING_ITEM tying skids/scrap/reject coils/warehouse items to a packing_list. Shipment is header-only, so a packing list cannot be assembled — core of dispatch, absent. |
| C | Outbound EDI generation pipeline (ediprocess) | missing | `cron/db01-prod/scripts/abis_scripts/ediprocess.sh` | The scheduled sqlplus batch that generates all transaction sets. No generation, no scheduler. |
| C | Packing tickets & BOL / combi-form printing (rpabco) | missing | `rpabco/d_report_abco_combi_form.srd` | The shipping-document engine: per-customer combi forms, packaging tickets, BOL, packing lists, BOL totals. Greenfield produces none. |
| C | 856 ASN generation on dispatch | missing | `edi/f_edi_856.srf` | Per-customer/enduser ASN generators fired on shipping a packing list. Greenfield "dispatch" is a status-only PATCH firing no EDI; the 856 trigger fields aren't even carried. |
| H | 861 Receiving Advice generation (multi-partner) | partial | `cron/edi-procs/p_create_edi_861_for_all.sql` | Build full X12 861 envelopes per BOL (Novelis/Constellium/Commonwealth/Aleris), write S_*.edi, insert outbound_edi_transaction, update inbound_shipment_status. Greenfield stub returns "deferred". |
| H | 870 order/coil-status EDI (Aleris/Wise/Alcan) | missing | `cron/edi-procs/edi_aleris_870.sql` | Per-partner 870 procs populating EDI_870_COIL_STATUS. None. |
| H | 846 Inventory Advice (Cleveland-Cliffs CCSC) | missing | `cron/edi-procs/p_846_cleveland_cliff_ccsc.sql` | 846 build using ABIS_X12_COIL/SKID/SCRAP mapping tables. Greenfield lacks the proc and the X12 mapping tables. |
| H | Inbound EDI ingestion (856 ASN parse) | missing | `coil_receiving/d_coil_list_by_bol_edi.srd` | Parse inbound .edi into inbound_shipment/inbound_coil/status (feeds the 861 pipeline). Receiving is manual BOL entry only. |
| H | 997 Functional Ack matching + aging alert | missing | `cron/edi-procs/p_check_997.sql` | Scan for sends >2h with no FA, alert. Greenfield surfaces `fa_receive_status` but has no matcher/ingest. |
| H | Sketch image (the actual drawing) | partial | `oracle_ddl.sql:9231` | Display is DONE — served from **`sketch_jpg`**, the live table (`sketch` is the retired BMP predecessor, and the two were re-keyed against each other: reading it gave 3,420 jobs a different part's drawing). **Upload still missing:** the image is never written through the API. |
| H | Sketch linkage to job/part + on-screen display | **BUILT** | `coil_eval/u_tabpg_job_sheet.sru:78` | Rendered on the production folder, the DAS console and the job sheet. Assigning a sketch to an order is still order-entry work. |
| H | Die → shape + per-shape line assignment | missing | `die_tool/w_die_new.srw` | Which shapes a die produces + line_die_4sheet_type mapping. Greenfield Die is a flat header, so scheduling can't know which line/die makes which shape. |
| M | 863 test-result / cert EDI screen | missing | `edi/w_edi_863.srw` | Full 863 send window (see §4). |
| M | Manual EDI send / resend from UI | missing | `edi/w_edi.srw` | Operator "Send Alcan 870/863" triggers. `/edi` is strictly read. |
| M | View archived EDI file contents | missing | `edi/w_display_edi_file.srw` | Open raw X12 from edi_out_file. Greenfield deliberately hides payloads — operators can't inspect an envelope. |
| M | EDI config maintenance (customer_edi/edi_type/X12 maps) | partial | `edi/d_customer_edi_list.srd` | Create/update trading partners, versions, X12 maps, per-customer flags. Only read-only lookups exist — partner onboarding is unbuildable. |
| M | Truck appointment / dock scheduling | missing | `oracle_ddl.sql:13671` | TRUCK_APPOINT_SHIPPING/TRUCK_LOCATION carrier appointments + load staging. No endpoint. |
| M | Shipment header field coverage | partial | `oracle_ddl.sql:9060` | Add the ~18 dropped columns incl. all EDI-trigger fields (edi_req/triggered/file_id_856/desadv…) — these are *why* dispatch can't drive the 856. |
| M | Die label / report printing | missing | `die_tool/d_die_print.srd` | Die labels/reports. |
| M | In-app EDI schedule definition | missing | `edi/w_define_schedule.srw` | Day/month/time schedule store (Admin #6). |
| L | Shipment status-change history (SHIPMENT_TRACK) | partial | `oracle_ddl.sql:9117` | Per-shipment status transitions with before/after + modified_by. Generic audit middleware isn't a substitute. |
| L | Carrier record field coverage | partial | `oracle_ddl.sql:1134` | Add carrier_street/zip/country/note/DUNS (DUNS matters for ASN carrier ID). |

---

## 6. Scanners (handheld RF coil-receiving CGI) — 14 gaps

Entirely greenfield-absent surface. The modern `scan.ts` is a generic shop-floor event logger in a *different* domain; none of the RF coil-receiving station exists.

| Sev | Feature | Status | Legacy | What greenfield needs |
|---|---|---|---|---|
| C | Handheld-optimized coil-receiving scan page | missing | `web/db01-prod/cgi-bin/coil_receiving_12.pl:59-93` | An RF-terminal page: auto-focus, masked barcode input, minimal HTML, that takes a customer coil barcode, resolves/mints an ABC number, and prints a label. All ~35 greenfield pages are desktop-only. |
| C | INBOUND_COIL_STATUS barcode→ABC lookup + mint-decision | missing | `coil_receiving_12.pl:95-196` | Model INBOUND_COIL_STATUS (DDL 4394-4421, trigger-populated) and branch already-minted→reprint vs unminted→mint. The table isn't in the greenfield schema — greenfield uses a different clerk-driven receiving_bol_coil path. |
| C | Network label printing to Zebra (ZPL over TCP :6101) | missing | `coil_receiving_12.pl:319-412` | Raw ZPL/CPCL over a socket (Code128, "Coil ABC #", "INSPECTED BY", 2 copies). No print/socket capability anywhere in api/ — the modern stack cannot physically emit a coil label. |
| H | On-demand ABC mint from a single scan | partial | `coil_receiving_12.pl:331-343` | Single-coil scan-triggered mint (SEQ.NEXTVAL + UPDATE INBOUND_COIL_STATUS + COMMIT). Greenfield mints in a desktop *batch* over receiving_bol_coil — the per-scan mint-and-print path is missing. |
| H | Coil-detail resolve by scanned customer coil number | partial | `coil_receiving_12.pl:198-317` | A lookup keyed by the scanned coil_org_num (BOL/net/gross/alloy/temper/gauge/width/lot/pack_id). GetCoilsAsync filters by status/alloy/location/customer only — no coil_org_num filter; plus "000000"→NO BARCODE and NONE fallback. |
| H | QR-code capture (validate + upsert BARCODE_STRING) | missing | `coil_receiving_12.pl:506-568` | Second-scan mill QR string validation + MERGE into BARCODE_STRING + copy to INBOUND_COIL_STATUS. Neither the table nor an endpoint exists (also missing on the desktop w_qr_manual side). |
| M | S-header barcode strip + validation | missing | `coil_receiving_12.pl:100-106, 201-207` | Strip the physical "S" prefix from scan-gun output before lookup; flag non-S as "NOT valid". No greenfield code accepts scan-gun input. |
| M | Printer routing by scanner device IP | missing | `coil_receiving_12.pl:14-21` | remote_addr→printer map (scanners .8/.9/.10 → printers .12/.13/.14) so each handheld prints to the Zebra next to it. |
| M | Printer connectivity check + offline handling | missing | `coil_receiving_12.pl:323-325, 390-411` | Ping the target printer before opening the socket; show a retry page if unreachable. |
| M | Reprint labels for an existing ABC number | missing | `coil_receiving_12.pl:414-469` | Re-send ZPL for an existing COIL_ABC_NUM (no sequence bump, no DB write). |
| M | Coil-defect email notification | missing | `coil_receiving_12.pl:471-504`; proc `oracle_ddl.sql:34518` | "Defects Notification" → P_SEND_EMAIL_COIL_DEFECT SMTP blast to 6 QA/receiving staff. |
| L | Operational error email on DB/socket failure | partial | `coil_receiving_12.pl:570-589` | Functionally superseded by structured ProblemDetails + X-Request-Id observability, but the specific "email support on scanner error" behavior is not reproduced. |
| L | Print no-coil-barcode placeholder label ("000000") | missing | `save/coil_receiving_add.pl:414-435` | Placeholder label for coils with no readable barcode. (Archived variant.) |
| L | Print a bundle of 50 sequential ABC labels | missing | `save/coil_receiving_add.pl:437-464` | Bulk label pre-print, incrementing ABC. (Archived variant.) |

---

## 7. Platform & Admin (security, OPC log, maintenance, reports, scheduler/services) — 31 gaps

| Sev | Feature | Status | Legacy | What greenfield needs |
|---|---|---|---|---|
| C | Feature-gate ENFORCEMENT across all windows | partial | `w_order_entry.srw:2532` (+~40 windows call `f_security_door`) | Wire `RequireFeatureAsync` to every domain endpoint. It is enforced on only 7 security-admin writes today; every other endpoint is auth-only. Biggest security-parity gap (NEXT_STEPS #2). |
| C | #6 ABIS-native scheduler + cron import | missing | `cron/…/crontab.oracle11g.txt`; `ADMIN_SUBSYSTEM_PLAN.md #6` | `/api/admin/jobs`, a scheduler engine, `abis_scheduled_job`/`abis_job_run` tables, run history, enable/disable/run-now. Design-only; all cron still runs solely on the DB host. |
| C | Active OPC data collector / poller | missing | `opc_log/w_opc_log_main.srw:85` | The edge/UA engine that *produces* the opc_log (timer-driven DA reads). Greenfield only reads the table — flag so the viewer isn't mistaken for coverage. |
| H | Delete user (with grant/membership safety checks) | missing | `security/w_user_managment.srw:236` | `DELETE /security/users/{id}` blocking on existing app-grant/group rows. Users can be created but never removed. |
| H | Update existing user (names/notes/active) | missing | `security/w_user_detail.srw:324` | `PUT/PATCH /security/users/{id}`. Create-only today. |
| H | Delete group | missing | `security/w_group_managment.srw:458` | `DELETE /security/groups/{id}`. |
| H | Admin set/reset user password | missing | `security/w_user_detail.srw:337` | An admin credential-reset path. No password concept exists (intentional if fully OIDC, but no reset path today). |
| H | Automated EDI generation on a schedule (861/870/846) | missing | `abis_scripts/ediprocess.sh` + edi-procs | The scheduled per-partner generation pipeline. Only a single 861 stub exists. (Must stay legacy-owned per no-live-firing.) |
| H | EDI transport — SFTP send/receive + postpro | missing | `GXS.ksh` + `GXS2.ksh` | Transport component incl. ISA/GS control-number mgmt and test-envelope rewrite. None. |
| H | EDI inbound processing + 997/FA reconciliation | missing | `abis_scripts/check_997.sh` + `p_check_997.sql` | Inbound 856/997 ingest + fa_receive_status reconciliation. Read-only ledger only. |
| H | EDI setup/admin UI (partners/types/versions/flags/maps) | missing | `edi/w_define_schedule.srw`, `w_edi.srw`; ADMIN #8 | Create/update of EDI partners, types, per-customer flags, VAN config. Only dropdown reads exist. |
| H | #7 Server/service admin console | missing | `ADMIN_SUBSYSTEM_PLAN.md #7` | `/api/admin/services` (status/health, log tail, safe restart, read env/install.state). Design-only. |
| M | Self-service password change | missing | `security/w_user_password.srw:228` | Old-pw verify + change. End users can't change their credential in-app. |
| M | Oracle DB account provisioning tied to app users | missing | `security/w_user_new.srw:327` + `w_user_managment.srw:99` | Acknowledge/replace the legacy coupling where each ABIS user is an Oracle account (by-design shift to OIDC, but lifecycle coupling dropped). |
| M | Step-up re-authentication popup | **n/a — dead in legacy** | `security/w_security_check.srw`; only caller `w_group_managment.srw:142` is **commented out** | Audited 2026-08-21. The elevation prompt was replaced by a flat "Insufficient privileges!" + close, which `RequireFeatureAsync`'s 403 already matches. Its password check opened a second Oracle connection **as that user** (`@db02_abc01`) — impossible under the by-design shift to AD + one service account (see the row above). Building it would be new capability, not parity. |
| M | Create logging event + OPC item-selection config | missing | `opc_log/w_opc_log_item_selection.srw` | Admin UI to define which OPC items get logged + poll interval. |
| M | OPC source/host/device tree + item-history drilldown | partial | `opc_log/w_opc_log_main.srw:461` | Hierarchical log>host>device>item navigation + item history on select. Greenfield returns flat lists. |
| M | Preventive-Maintenance (PM) scheduling subsystem | missing | `maintenance/w_maint_pm_management.srw` (+new/list) | Whole PM area (definitions, formats, PM list). None. |
| M | Maintenance parts / spares inventory | missing | `maintenance/w_maint_parts_management.srw` | Parts catalog, categories, vendors, units, images. None. |
| M | Maintenance equipment hierarchy + More-Details | partial | `maintenance/w_maint_logs_management.srw:325-399` | System/subsystem/item-device cascade + More-Details editor. Only group-department is exposed. |
| M | DB-host cron view + in-database job control | missing | `ADMIN #7` + `JOB_INVENTORY.md` | `/api/admin/host/cron` + DBMS_SCHEDULER enable/disable/run-now for stay-on-DB jobs. |
| M | Scheduled shop-floor business jobs (shift-end, unmatched-coil) | missing | `abis_scripts/check_shift_end.sh`, `check_status_unmatched_coil.sh` | Automated homes for shift-end processing + periodic unmatched-coil (query exists as manual report only). Blocked on #6. |
| M | Scheduled recovery + inventory report generation | missing | `abis_scripts/create_recovery_report.sh`, `create_inventory_report.sh` | Automated recovery-report job (data exists under /quality; the job doesn't). |
| M | EDI transaction monitor with retry + failure alerting | partial | `w_display_edi_file.srw` + `846_email.sh` | Retry/resend + failure alerting. Read-only status browsing is built. |
| M | Downtime analytical breakdowns (job/day/month/year/part/shift) | partial | `downtime/w_downtime_detail_monthly.srw` (+15 views) | The many pivoted downtime analyses + downtime-vs-production ratio. One report exists. |
| M | Uptime reports (uptime, uptime-per-line) | missing | `downtime/w_report_uptime.srw` | The uptime counterparts to the downtime report. |
| M | Maintenance log record navigation + Find | partial | `maintenance/w_maint_logs_management.srw:401-629` | Record-stepping (first/prior/next/last) + dedicated log search. Paged list partly substitutes. |
| M | Maintenance reports (parts, PM list, log print) | missing | `maintenance/w_report_maint_parts_management.srw` | Not among the 16 greenfield reports. |
| M | Production scheduling reports (shift schedule/status) | missing | `daily_prod/w_daily_prod_report_shift_schedule.srw` | Tied to the un-rebuilt production-scheduler area. |
| M | Weekly / alphabetical production reports + Excel export | missing | `daily_prod/w_daily_prod_report_weekly_alph.srw` | Weekly/alph rollups + native Excel (greenfield exports CSV only). |
| L | User account-status flags (must-change-pw / locked / etc.) | missing | `security/w_user_detail.srw:606-670` | These checkboxes were display-only (Enabled=false) in legacy — no live behavior lost, but no greenfield home for the fields. |

---

## Must-build for full parity (prioritized)

**P0 — Foundational primitives & core operational blockers.** Without these the modern stack cannot run the business, and each unblocks many downstream gaps:

1. **Shape geometry primitive** — model + read/write the 11 shape dimension tables once, then consume it in order entry, part master, piece-weight calc, invoice spec, dimension-QC tolerances, the DAS job sheet, and the stacker board. (Closes gaps across §1, §3, §4, §5.)
2. **Document / print engine** — a shared output service for invoices, quotes, packing lists / BOLs / combi-forms, skid & scrap tags, coil ZPL/CPCL labels, and transfer certificates. Nothing physical comes out of the system today.
3. **Live DAS data acquisition** — the edge/UA-to-Classic-DA bridge, `LINE_CURRENT_STATUS` line board, current-coil binding, shift/coil lifecycle (SHIFT_COIL), and the Operation Panel workflow. The defining capability of the shop floor; currently absent.
4. **Handheld scanner coil-receiving station** — the RF page, the `INBOUND_COIL_STATUS` / `BARCODE_STRING` model, single-scan mint, QR capture, and network Zebra printing. An entire mandated surface with zero greenfield presence.
5. **EDI engine, single-owner** — generation (861/870/846/856/863), VAN transport, inbound ingestion, and 997 reconciliation. Largest functional block; must be delivered as a controlled cutover under one owner per the no-live-firing rule, not a parallel firing path.
6. **App-wide feature-gate enforcement** — wire the existing gate to every domain endpoint (today: 7 admin writes only). Highest-value security fix and low effort relative to impact.

**P1 — Core business writes & workflows (currently read-only or hollow):**

7. Quote authoring/editing + the real pricing/cost model (CirclePro job cost + $/lb; SheetPro), and quote save/print/email.
8. Invoice document generation + save (number/date) + print, and the rejected-coil billed-weight rule.
9. Order editing/copy in-UI, customer-coil assignment, and the order-entry part picker.
10. Part revisions, routing sequences, delete, copy, and obsolete-in-use guard.
11. Customer master full field coverage + EDI/behavior flags + delete.
12. Packing-list line items + shipment EDI-trigger fields (prerequisite for the 856).
13. Recovery scrap-worksheet write + setup + the report suite; 863 test-result write + Instron import.
14. QA hold workflow (+ COIL_TRACK_QA) and dimension-check tolerance validation (the actual quality gate).
15. Warehouse/skid CRUD, scrap-skid lifecycle, and correct coil-ownership-transfer mint semantics (+ status-12 setting).
16. Security lifecycle: delete/edit users & groups, admin password reset (or explicit OIDC decision).

**P2 — Operational depth, scheduling & analytics:**

17. Admin subsystems #6 scheduler/cron-import, #7 service console, #8 EDI admin/config UI.
18. Downtime cause taxonomy (DT_INSTANCE_DETAIL) + automatic downtime capture + operator prompts.
19. Reports parity — uptime, downtime pivots, weekly/alphabetical production + Excel, maintenance/PM, scheduled recovery/inventory.
20. OPC log collector/edge bridge; serial scale/gauge acquisition; stacker automation.

**P3 — Nice-to-haves and superseded items:** sales calendar/catalog/preferences; sanity warnings (received-date, feed-length nag); reprint pickers & archived-BOL browsers; account-status checkboxes (were display-only); scanner error-email (superseded by structured observability).

---

### Honest severity notes for the team
- **Two P0 items are architecturally gated, not just unwritten:** live DAS (Classic OPC DA/DCOM needs an edge bridge) and the EDI engine (no-live-firing → must be a single-owner cutover). Budget these as programs, not tickets.
- **"Read-only viewer" masquerades as coverage** in several places — EDI, OPC log, recovery/quality lookups, warehouse, sketches. The data model or a GET exists, so a surface can look "done" while the entire write/workflow half is missing. The partials are frequently more dangerous than the missings because they invite that assumption.
- **Silent-correctness risks** (no crash, wrong number): rejected-coil billed weight summed naively in the browser; dimension-check `in_spec` stored without validation; edge-trim tolerance saved out-of-range with no gate. These can bill or ship wrong material and should be treated above their apparent size.