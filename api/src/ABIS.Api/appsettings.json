{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Database": {
    "//": "Provider is Sqlite or Oracle. For production set Provider=Oracle and supply a real ConnectionString (e.g. via environment variable Database__ConnectionString or a secret store). Seed must be false against a real database.",
    "Provider": "Oracle",
    "ConnectionString": "",
    "Seed": false,
    "//Sequences": "Per-table Oracle sequence overrides for names that don't follow {id_column}_seq. Each value was confirmed to exist in ALL_SEQUENCES on the live DB (2026-07-24) — a missing/misnamed one is ORA-02289 on the first insert, which SQLite CI can't catch. error_evt/return_scrap_item/sheet_skid_dimension_check/coil_ownership_transfer were added after the write paths were validated against the live .230 DB.",
    "Sequences": {
      "customer_contact": "customer_contact_id_seq",
      "dt_instance": "dt_instance_seq",
      "scan_log": "scan_log_seq",
      "shipment": "packing_list_num_seq",
      "error_evt": "error_evt_seq",
      "return_scrap_item": "return_scrap_item_id_seq",
      "sheet_skid_dimension_check": "sheet_skid_dimension_check_seq",
      "coil_ownership_transfer": "coil_owner_transfer_certif_seq"
    },
    "//MaxIdTables": "Tables whose id is assigned MAX(id)+1 (no Oracle sequence) — the legacy behaviour. The security admin tables have no *_id_seq in the legacy schema (PB assigned user_id/group_id/application_id via MAX+1), so listing them here avoids ORA-02289 on create. The AbisSchema-provisioned tables (sales-quote + abis_* owned tables) create NO sequence, so every one that assigns its id via NextIdAsync must be listed here or it 500s on Oracle (ORA-02289). abis_truck_appointment is the concrete case; abis_scheduled_job/abis_job_run currently use inline MAX+1 but are listed for safety.",
    "MaxIdTables": [ "maint_log", "pm", "pm_actions", "pmcompletions", "security_user", "security_group", "security_application", "sales_quote", "sales_reminder", "sales_probability", "abis_truck_appointment", "abis_scheduled_job", "abis_job_run", "dt_instance_detail" ]
  },
  "Board": {
    "//LineOrder": "The floor board's card order, by line_num, as the plant reads the floor (BL 84, BL 78, BL 110, BL 108, BL 36, BL 24 — user 2026-08-04). It is not numeric, alphabetical or activity-based, so it lives here rather than in a sort() a later reader would 'correct' into ascending. A line NOT listed still appears, after the listed ones.",
    "LineOrder": [ 7, 4, 6, 5, 2, 1 ],
    "//DecommissionedLines": "Lines that no longer exist on the floor (line_num). BL 60 = 3, decommissioned 2026-08-04; 3 is its real code on the live LINE table (verified on .230). Kept SEPARATE from LineOrder on purpose: forgetting to list a line in the order must not hide it. TWO effects: such a line drops off the floor board, AND it refuses NEW work — no new job, shift or downtime instance can be booked to it, and the DAS operations that assign work (current-job, current-coil, shift/start, coil-run/start, change-job, queue) answer 409. History is untouched: BL 60 carries 1,163 jobs on live and every one keeps saying so, the lookup still returns the line, and editing an existing record on it still works. Winding down stays available too — shift/end, coil-run/end, reverse and queue removal are NOT blocked, because BL 60 left 9 open shift rows and a queued job behind and that state has to be clearable.",
    "DecommissionedLines": [ 3 ]
  },
  "Documents": {
    "//TheoreticalWeightCustomers": "Customers billed on THEORETICAL weight, so the combi form's net column carries prod_item_theoretical_wt instead of prod_item_net_wt. Legacy hard-coded customer_id = 2802 (TOYOTA TSUSHO AMERICA, 50 shipments on live) into four of its combi sheet-detail DataWindow queries. It is a commercial arrangement, so it lives here rather than in SQL — a customer joining or leaving it is a business change, not a code change.",
    "TheoreticalWeightCustomers": [ 2802 ]
  },
  "ApiKeys": {
    "//": "Supply keys via configuration/secret (e.g. ApiKeys__Keys__0). Set Enabled=false only on a trusted internal network.",
    "Enabled": true,
    "HeaderName": "X-Api-Key",
    "Keys": []
  },
  "Scheduler": {
    "//": "Scheduler EXECUTION engine. OFF by default (the /admin/jobs registry stays view/define only). Even when Enabled=true it ONLY runs allowlisted in-process operations (noop, heartbeat, …) — there is no shell/legacy path, so it can never fire the legacy EDI/cron work (single-owner guardrail). Set Enabled=true only after adding the operations you actually want scheduled.",
    "Enabled": false,
    "PollSeconds": 30
  },
  "WinSpc": {
    "//": "Read-only connector to the plant WinSPC (SQL Server) quality DB on RSEDAM-PC (192.168.10.143, static TCP 1433). Disabled by default. To enable: set Enabled=true and supply a read-only ConnectionString. Recommended (good-citizen on the live SPC box): WinSpc__ConnectionString='Server=192.168.10.143,1433;Database=WinSPC;User Id=abis_ro;Password=...;TrustServerCertificate=True;Connect Timeout=5;Max Pool Size=5'. Provider=Sqlite is for the test mimic only. Job/Coil tag patterns match WinSPC's free-text tag names. CommandTimeoutSeconds caps any single read.",
    "Enabled": false,
    "Provider": "SqlServer",
    "ConnectionString": "",
    "JobTagPattern": "%JOB%",
    "CoilTagPattern": "%COIL%",
    "CommandTimeoutSeconds": 15
  },
  "Audit": {
    "//": "Set Enabled=false when the target schema has no compatible audit table (the live DBO schema has no opc_action_log). Otherwise auditing self-disables after the first write failure.",
    "Enabled": true
  },
  "Email": {
    "//": "TEST PHASE: OverrideRecipient redirects EVERY email (automated/triggered/manual) to this one inbox — real recipients are preserved in the subject/body. Clear it (empty) ONLY at the production cutover. Smtp.Host empty = log-only (nothing sent); set it to the plant relay to actually deliver.",
    "OverrideRecipient": "cmattinson@albl.com",
    "FromAddress": "abis@albl.com",
    "FromName": "ABIS",
    "//SendTestOnStartup": "Set true (e.g. Email__SendTestOnStartup=true) to send one diagnostic email through the full pipeline at startup — a deploy-time smoke test. Logs the redirect when Smtp.Host is empty; delivers once the relay is set. Default off.",
    "SendTestOnStartup": false,
    "Smtp": {
      "Host": "",
      "Port": 25,
      "UseSsl": false,
      "User": "",
      "Password": ""
    }
  }
}
