<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>labelprint</AssemblyName>
    <RootNamespace>LabelPrint</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- References the API project ON PURPOSE. A test print has to exercise the SHIPPED label builder;
         a copy of the layout in this tool would prove only that the copy works. -->
    <ProjectReference Include="..\..\api\src\ABIS.Api\ABIS.Api.csproj" />
  </ItemGroup>

</Project>
